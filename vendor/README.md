# 对照快照（只读）

`pi-mono-a74c5da/` 是 [badlogic/pi-mono](https://github.com/badlogic/pi-mono) 的 Git 首个提交，冻结在：

- 提交：`a74c5da112c29466f182a03108337a488c785d76`
- 日期：2025-08-09
- 说明：`Initial monorepo setup with npm workspaces and dual TypeScript configuration`

本目录只用来对照阅读，不要改这里的文件。我们自己的实现写在仓库根的 `reconstruct/`。

本阶段只关心 `pi-mono-a74c5da/packages/agent/`。`packages/tui` 与 `packages/pods` 先不读。
